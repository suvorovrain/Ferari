use atlas::Atlas;
use State;

pub struct Render {
    pub entity_atlas: Atlas,
    pub world_buf: Vec<u32>,
}

impl Render {
    pub fn new(world_buf: Vec<u32>, entity_atlas: Atlas) -> Self {

    }
    pub fn init(&self,) {

    }
    pub fn render(self, )
}